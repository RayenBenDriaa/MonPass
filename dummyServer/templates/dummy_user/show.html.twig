{% extends 'base.html.twig' %}

{% block title %}DummyUser{% endblock %}

{% block body %}
    <h1>DummyUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dummy_user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ dummy_user.email }}</td>
            </tr>
            <tr>
                <th>Cin</th>
                <td>{{ dummy_user.cin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dummy_user_index') }}">back to list</a>

    <a href="{{ path('dummy_user_edit', {'id': dummy_user.id}) }}">edit</a>

    {{ include('dummy_user/_delete_form.html.twig') }}
{% endblock %}
